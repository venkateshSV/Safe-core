import * as React from 'react';
export type ExplorerButtonProps = {
    title: string;
    href: string;
};
declare const ExplorerButton: ({ title, href, }: ExplorerButtonProps) => React.ReactElement | null;
export default ExplorerButton;
