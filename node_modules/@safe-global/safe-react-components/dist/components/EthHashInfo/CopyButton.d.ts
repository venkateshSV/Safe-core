import * as React from 'react';
type CopyButtonProps = {
    text: string;
    className?: string;
    children?: React.ReactNode;
    initialToolTipText?: string;
    ariaLabel?: string;
    onCopy?: () => void;
};
declare const CopyButton: ({ text, className, children, initialToolTipText, onCopy, }: CopyButtonProps) => React.ReactElement;
export default CopyButton;
