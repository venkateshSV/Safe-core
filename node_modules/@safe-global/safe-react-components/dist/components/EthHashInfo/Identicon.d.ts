import * as React from 'react';
type IdenticonProps = {
    address: string;
    size?: number;
};
declare const Identicon: ({ address, size, }: IdenticonProps) => React.ReactElement;
export default Identicon;
