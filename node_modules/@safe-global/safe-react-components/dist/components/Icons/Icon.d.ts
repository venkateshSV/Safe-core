import * as React from 'react';
import SvgIcon from '@mui/material/SvgIcon';
type IconProps = {
    component: typeof SvgIcon;
};
declare const Icon: ({ component }: IconProps) => React.ReactElement;
export default Icon;
