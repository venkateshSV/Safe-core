import * as React from 'react';
type CopyAddressButtonProps = {
    prefix?: string;
    address: string;
    copyPrefix?: boolean;
};
declare const CopyAddressButton: ({ prefix, address, copyPrefix, }: CopyAddressButtonProps) => React.ReactElement;
export default CopyAddressButton;
