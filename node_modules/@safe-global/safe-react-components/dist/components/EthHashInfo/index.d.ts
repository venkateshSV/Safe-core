import * as React from 'react';
import { ExplorerButtonProps } from '../ExplorerButton';
export type EthHashInfoProps = {
    address: string;
    chainId?: string;
    name?: string | null;
    showAvatar?: boolean;
    showCopyButton?: boolean;
    prefix?: string;
    showPrefix?: boolean;
    copyPrefix?: boolean;
    shortAddress?: boolean;
    customAvatar?: string;
    hasExplorer?: boolean;
    avatarSize?: number;
    children?: React.ReactNode;
    ExplorerButtonProps?: ExplorerButtonProps;
};
declare const EthHashInfo: ({ address, customAvatar, prefix, copyPrefix, showPrefix, shortAddress, showAvatar, avatarSize, name, showCopyButton, hasExplorer, ExplorerButtonProps, children, }: EthHashInfoProps) => React.ReactElement;
export default EthHashInfo;
